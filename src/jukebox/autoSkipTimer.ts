// update clock of audioplayer (jukebox)
// run next() of jukebox when timer > track duration

// function autoSkipTimerSystsem(dt: number) {
//     // iterate over all entiities with a Transform
//     console.log()
//   }

//engine.addSystem(moveSystem)
